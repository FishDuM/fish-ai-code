spring:
  application:
    name: fish-ai-code
server:
  port: 8911
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: hk.ljx.fishaicode.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
